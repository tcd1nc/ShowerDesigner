<Window x:Class="YankeeShower.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YankeeShower.Views"

        BorderThickness="1"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="Registration" Height="220" Width="400">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Bottom" Height="32">
            <Button Content="Register" x:Name="register" Click="register_Click" Width="70" Height="22"/>
        </Grid>

        <StackPanel Orientation="Vertical" Margin="0,10,0,10">
            <TextBlock Text="The registration has expired. Please enter the new registration code from the software developer" TextAlignment="Left" TextWrapping="Wrap" Height="50" Width="200"/>
            <TextBox x:Name="registrationcode" Width="300" Height="20"/>
        </StackPanel>
    </DockPanel>
</Window>
